// Material Symbols (Outlined) — the suite-wide icon set.
//
// Usage convention:
//   <span className="material-symbols-outlined text-[16px] leading-none">icon_name</span>
// `icon_name` is any glyph name from the Material Symbols catalog:
// https://fonts.google.com/icons?icon.set=Material+Symbols&icon.style=Outlined
//
// Consuming apps load the stylesheet once (e.g. a <link> in the root layout/head)
// using MATERIAL_SYMBOLS_STYLESHEET_URL below, then reference glyphs by name in
// className as shown above — no per-icon import needed.

export const MATERIAL_SYMBOLS_STYLESHEET_URL =
  'https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200'

// Class name convention, exported so consumers don't have to hardcode the string.
export const materialSymbolsOutlinedClass = 'material-symbols-outlined'
